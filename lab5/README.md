# Lab 5 — Lifetimes, Generics & Modules

- `exerciseA_lifetimes.rs` — Lifetime annotations, longest(), Important struct, first_sentence()
- `exerciseB_generics.rs` — Generic functions with trait bounds, Pair<T>, zip_with()
- `src/` — Full module project demonstrating pub/private visibility (geometry, shapes, transforms, utils)
