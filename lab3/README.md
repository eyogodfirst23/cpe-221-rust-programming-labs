# Lab 3 — Structs, Traits & Error Handling

- `exerciseA_structs.rs` — BankAccount struct with deposit/withdraw (Result-based error handling)
- `exerciseB_traits.rs` — Describable and Area traits, Display impls, dynamic dispatch
- `exerciseC_errors.rs` — Custom AppError enum, From conversion, safe_div with DivisibleByZero variant
